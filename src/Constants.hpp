#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP


const float MIN_SCALE        = 0.5;
const float MAX_SCALE        =   5;
const float ZOOM_SPEED       = 0.2;

const float DOUBLE_CLICK_GAP = 0.2;
const float MOVING_DELAY     = 0.1;

const int   MAX_WIDTH        =  10;

#endif